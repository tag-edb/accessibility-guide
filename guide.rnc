start = chunks
chunks = element chunks {chunk+}
chunk = element chunk {id, title, (menu|recipe)+}
id = attribute id {xsd:ID}
title = element title {text}
menu = element menu {item+}
recipe = element recipe {item+}
item = element item {ref?, text}
ref = attribute ref {xsd:IDREF}
